# Worklog — 03 Aug 2026

## Summary

Full day on **Suki Tools Management** (standalone Next.js app on ERP SQL Server). Everything below is on GitLab `origin/main` (`https://git.sukierp.com/RoshiniV/suki_tools.git`) except this worklog file itself (untracked until committed).

### Commits pushed today (chronological)

| Time | Commit | Message | Scope |
|------|--------|---------|--------|
| 16:17 | `7245452` | feat: expand tools management with auth, masters, calibration, and reports | **Major demo-ready build** (largest ship of the day) |
| 16:22 | `7acbeef` | chore: remove root worklog markdown files | Cleanup of root `worklog-*.md` |
| 17:29 | `2a2f285` | fix: correct calibration KPIs, align cards, and add demo users | KPI correctness, demo seed, logo, layout |
| 17:51 | `22ec935` | fix: support partial calibration receive and prefill receiver | Partial calib receive + receiver name |

**HEAD = `22ec935` — branch `main` matches `origin/main`.**

---

## Part A — Earlier push (`7245452`) — demo-ready expansion

Ship login/session hardening, richer masters and tools workflows, calibration DC/PDF/results flows, reports/settings surfaces, and supporting Prisma migrations so the app is demo-ready end to end.

### A1. Standalone auth (not ERP password)

- `TOOLS_APP_USER` migration + Prisma `User` model
- JWT login/logout/me/verify (`src/app/api/auth/*`)
- Middleware session guard; `/login`, `/auth/session-expired`
- bcrypt password helpers, login rate guard, role permission matrix
- `prisma/seed.ts` bootstrap admin (env `SEED_ADMIN_*` + ERP audit actor code)
- ERP writes use `resolveErpAuditUserId` — app username ≠ `ERP_USER.USER_ID`

### A2. Prisma / storage

- Migration `20260801130000_create_tools_app_user`
- Migration `20260803113000_create_tools_app_document` (tool document metadata)
- Schema expansions for issue/receive/calibration/documents
- Theme-aware SVG logos under `public/logo-*.svg`

### A3. Masters

- Tools master major rebuild (create/edit/view, stock, calibration, preventive, specs, serials, machines, suppliers, import/export)
- Tools Group / Subgroup / Tool Types / Tool Mapping
- Suppliers / Subcontractors
- Pricing master + Esskay price JSON seed data
- Reorder level, calib frequency, gauge types, lookups hub
- Document upload panel (`ToolDocumentsPanel` + `/api/tools/documents`)

### A4. Transactions

- Tool Issue (`GAUGE_TOOLS_ISSUE`) — create DC, lines, search/history, KPIs
- Tool Receive — open DC picker, GRN history, pagination/search
- Customer Receive — filtered view where `CUST_CODE` present
- Consumption / requisition-pending surfaces

### A5. Calibration

- Calibration Issue — DC create, history, PDF download, documents
- Calibration Receive — post lab return against open DC
- Calibration Results Update + export
- Calibration Due List
- Overview hub for calibration

### A6. Reports / History / Settings / PO-linked

- Reports hub + category APIs (tools, calibration, suppliers, subcontractors, tools-history, etc.)
- Tools History Card module (status, holder, issue, receive, calib, results, GRN)
- Settings: company, branches, users/roles/permissions stubs, audit trail, numbering, notifications
- PO-linked receive/schedule/purchase-order pages

### A7. Docs added with that push

- `docs/demo-click-guide.md`
- `docs/end-to-end-practice-flow.md`
- `docs/masters-calibration-flow.md`
- `docs/module-wise-testing-guide.md`
- `docs/reports-and-analytics.md`

### A8. UX shell

- Sidebar / TopBar / themes / SuccessOverlay / StatusBadge / AnimatedCountUp
- Module KPI row pattern across screens
- Page loaders / navigation loader

---

## Part B — Cleanup push (`7acbeef`)

Removed root-level historical worklogs (moved away from repo root clutter):

- `worklog-2026-07-26.md`, `worklog-2026-07-26-manpro-sync.md`
- `worklog-2026-07-29.md`, `worklog-2026-07-31.md`, `worklog-2026-08-01.md`

*(Today’s log lives under `docs/worklog-2026-08-03.md`.)*

---

## Part C — Afternoon session (`2a2f285` + `22ec935`)

### C1. Demo users (`TOOLS_APP_USER` only)

- Script: `prisma/seed-demo-users.ts` → `npm run db:seed:demo-users`
- Idempotent upsert; no `ERP_USER` touch; System Admin untouched
- Role: **Tools Admin**

| Username | Password |
|----------|----------|
| `demo1` | `DemoUser1!` |
| `demo2` | `DemoUser2!` |
| `demo3` | `DemoUser3!` |

### C2. Calibration Issue KPI data fix

**Bug:** Due = Overdue = 142; Under Calibration = 0.

**Cause:** KPIs reused due-list picker (excludes in-lab; Due counted overdue too).

**Fix:** Split `dueSoonCount` vs `overdueCount`; live Under Calibration from master status; DC slips = history list length; clearer badges/tooltips.

### C3. KPI card visual alignment

Equal-height `ModuleKpiRow` / `KpiRow` (label band, value row, reserved footer). Shortened Tools Master / Calib Issue subtexts.

### C4. Login logo

Switched from black-backed PNG to `/logo-blue.svg`.

### C5. Tools Issue `receiveNameTwo` error

Stale Prisma client in long-running Next process. Regenerated client + restarted `:3000`. Column exists on `GAUGE_TOOLS_ISSUE`.

### C6. Partial Calibration Receive

**Bug:** Receiving 2 of 4 lines closed whole DC → remaining tools vanished.

**Fix:** Status OPEN/PARTIAL/CLOSED from **line** state; `?awaitingReceive=1`; allow follow-up receives; prefills Receiver Name from login user; Item Value = Qty × Price copy.

### C7. Post-receive stage (tester Q&A)

| After receive, before Results Update | Value |
|--------------------------------------|--------|
| Tool master | **Available** |
| Issue line | **Received** + Pending result |
| Results Update | Still required for pass/fail + next due |

### C8. Also in `2a2f285` (bundled WIP)

Calibration receive/results-update page+API expansions, Tools Issue UI, Pricing master updates, validators / `calibResultsData`, ToolDocumentsPanel tweaks.

---

## Remote / push status

| Item | Status |
|------|--------|
| GitLab `origin/main` | Includes all 4 commits through `22ec935` |
| Local `main` | Clean match with `origin/main` |
| `docs/worklog-2026-08-03.md` | **Not committed / not pushed yet** |

---

## Outstanding / follow-ups

- [ ] Commit + push this worklog if required in-repo
- [ ] Smoke: partial calib receive remaining lines on existing DCs
- [ ] Smoke: Results Update after receive
- [ ] Smoke: Tools Issue create after Prisma restart on all tester machines
- [ ] Consider delaying tool master **Available** until Results Update (product decision)
- [ ] Rotate any GitLab PAT exposed during earlier push setup

---

## Quick tester handoff

```
Login: demo1 / DemoUser1!  (also demo2, demo3)
After Issue DC toast → Tools Issue list (search DC) or History → Issue History
Calibration flow: Issue → Receive (partial OK) → Results Update
```
