# Worklog — 05 Aug 2026

## Summary

Dashboard chart polish + React key fixes + design-system export + shared form/overlay UX on **Suki Tools Management**. Work is **local / uncommitted** at end of day (not pushed). Branch `main` tracking `origin/main`.

Primary theme: **consistent modal forms** (list stays mounted, URL changes with `?action=add` / edit, sectioned 2-col layout, searchable selects with chevron).

---

## Part A — Chart polish & dashboard KPI fixes

### A1. Monthly movements bar loading

- Pulsing diverging-bar skeleton + entrance animation on dashboard **Monthly Tool Movements**
- Shared CSS: `.bar-chart-skeleton-bar` in `src/app/globals.css`
- Component: `MonthlyMovementsBarChart.tsx`

### A2. All Tool Reports — Progress radial

- Replaced tools-by-group bar on `/dashboard/reports/tools` with concentric **Progress** rings (Available / Issued / Under Cal)
- Center %, legend, View Detail, loading skeleton — `ReportProgressChart` in `ReportCharts.tsx`

### A3. Shared bar chart effects

- Added `src/components/BarChartEffects.tsx` (`BAR_ANIMATION`, skeletons)
- Wired into `ReportBarChart`, overview velocity/histogram, monthly movements; suppliers/subcontractors pass `loading`

### A4. React key / data hygiene

| Issue | Fix |
|-------|-----|
| Duplicate key `"Available"` | Progress / ToolsByGroup keys; KPI merge of blank statuses → `"Available"` |
| `ActivityTable` missing `id` | Key on `dcNo`+index; map `receiveName` |
| KPI `recentActivity` / status & group merge | `src/app/api/dashboard/kpi/route.ts` |

### A5. Tools By Group dark mode

- Left panel hardcoded `bg-white` / slate → theme tokens (`--bg-card`, `--text-*`, donut stroke `var(--bg-card)`) in `ToolsByGroup.tsx`

---

## Part B — Design system export (read-only package)

Created **`design-system-export/`** (+ zip):

- Tokens, theme libs, UI/layout/charts copies
- Some copies stripped to props (`PageHeader`, `TopBar`, `QuickActions`, `RoleGate`)
- Docs: `README.md`, `DEPENDENCIES.md`, `NOTES.md` (APP-SPECIFIC flagged)
- Live app **not** rewritten for export — export folder only

---

## Part C — Shared form field system

### C1. Tokens & helpers

| Piece | Path |
|-------|------|
| Form primitives | `src/components/ui/form.tsx` — `FormLabel`, `FormInput`, `FormSelect`, `FormGrid`, `FormCard`, `FormModalSection` |
| CSS | `src/app/globals.css` — `.form-label`, `.form-control`, `.form-grid`, `.form-modal-section`, `.form-btn-cancel`, `.form-btn-save` |
| Visual language | Sentence-case labels above fields; white inputs + light border; 2-col grid; section titles with hairline dividers |

### C2. Rollout

Applied `form-label` / `form-control` / `form-grid` / `form-card` across masters and several transaction/PO pages (tools, issue/receive, suppliers, subcontractors, groups/subgroups/types, lookups, consumption, etc.).

---

## Part D — Overlay create/edit (URL-synced, list stays)

Pattern: **list stays mounted** → dimmed **`OverlayModal`** → URL updates → no separate full page.

### D1. Shared overlay

- New: `src/components/ui/OverlayModal.tsx`
  - Backdrop, circular close, Escape, body scroll lock
  - Sticky footer slot (Cancel / Save Now)
  - Sizes: `md` … `5xl`

### D2. Item/Asset Master (Tools)

- Path: `src/app/dashboard/masters/tools/page.tsx`
- **Add** → `?action=add`; **Edit** → `?action=edit&refNo=…`
- Create/edit form in overlay with stacked `FormModalSection`s (Tool details, Stock & flags, Calibration, Preventive, Technical, Serial units)
- Tabs replaced by scrollable sections inside modal
- Cancel / X / Escape / browser-back clear query params (dirty leave prompt retained)

### D3. Tools Issue

- Path: `src/app/dashboard/transactions/issue/page.tsx`
- **New Issue (DC)** → overlay + `?action=add`
- Sections: Who and what, Return terms, Transport, Additional details, Tool line items
- Footer: Cancel / Clear Form / **Save Now**

### D4. Tools Receive

- Path: `src/app/dashboard/transactions/receive/page.tsx`
- **Receive** → overlay + `?action=add`
- Sections: Open DC picker, Receive details (+ staged lines)
- Same Cancel / Save Now footer pattern

---

## Part E — SearchSelect (easy fetch + chevron)

### E1. Component

New: `src/components/ui/SearchSelect.tsx`

- Label above; type-ahead; clear + **chevron** arrow
- Focus ring; keyboard nav; selected chip with Change
- Matches “search tool / supplier” drawer-style pickers

### E2. Wired into

| Page | Fields |
|------|--------|
| Issue | Party (sub/supplier), in-stock tool picker |
| Receive | Sub contractor filter, Open DC/Tool line picker, Party Name |

---

## Files touched today (working tree highlights)

### New shared UI
- `src/components/ui/OverlayModal.tsx`
- `src/components/ui/SearchSelect.tsx`
- `src/components/ui/form.tsx` (expanded: `FormModalSection`, modal form tokens)
- `src/components/BarChartEffects.tsx`

### Charts / dashboard
- `MonthlyMovementsBarChart.tsx`, `ToolsByGroup.tsx`, `ActivityTable.tsx`, `KpiRow` / `ModuleKpiRow` consumers
- `ReportCharts.tsx`, `OverviewCharts.tsx`, reports tools/suppliers/subcontractors pages
- `src/app/api/dashboard/kpi/route.ts`
- `src/app/globals.css` (skeletons + form system)

### Overlay pages
- `src/app/dashboard/masters/tools/page.tsx`
- `src/app/dashboard/transactions/issue/page.tsx`
- `src/app/dashboard/transactions/receive/page.tsx`

### Design export
- `design-system-export/**`, `design-system-export.zip`

### Docs
- `docs/worklog-2026-08-05.md` (this file)

*(Plus earlier-day uncommitted carry-overs still in the tree: toast migration, GRN DataTable, PDF truncate, schema/package bumps, etc. — see 03–04 Aug worklogs.)*

---

## How to smoke-test (quick)

1. **Tools Manage** → Add Tool Record → overlay + URL `?action=add`; Cancel clears URL; list visible behind
2. **Tools Issue** → New Issue (DC) → overlay; party/tool via SearchSelect chevron; Save Now
3. **Tools Receive** → Receive → overlay; DC/tool SearchSelect; Save Now
4. Dashboard monthly bar skeleton; Reports → Tools Progress rings; dark mode Tools By Group panel

---

## Open / follow-ups

1. **Commit / push** — none of today’s (or prior uncommitted) code changes committed yet
2. Roll **OverlayModal + SearchSelect** to other masters that still use full-page or side-drawer create (suppliers, subcontractors, tool-mapping, etc.) if desired for full consistency
3. Optional: Next.js intercepting routes (`@modal`) instead of `?action=` query — current query approach works without parallel route folders
4. Design-system export: keep in sync when OverlayModal / SearchSelect / form tokens evolve
5. Live confirm Progress chart + KPI keys under real data / dark theme

---

*End of worklog — 05 Aug 2026. Stopping here for now.*
