# Worklog — 08 Aug 2026

## Summary

Full day on **Suki Tools Management**: closed major ERP gap items around **Purchase Orders (create + finance)**, **Pricing Master approve/reject**, **DB-backed role permissions / users**, and **GRN create reliability** (non-identity key allocation, list pagination, tool-number search). Bundled polish covers **Requisition Pending**, **Tool Journey timeline**, **Download Center**, **Calib Frequency CRUD**, and issue/requisition write-back.

Work is **committed and pushed** to `origin/main`.

**Primary themes**
1. Tools-owned PO create into shared ERP tables + Tools-owned payment/ledger finance tables
2. Pricing propose → approve/reject without mutating live `RATE` until approval
3. Settings: `TOOLS_ROLE_PERMISSION` matrix + users API (editable permissions)
4. Harden GRN / PO numbering (MAX+1, not Prisma `@default(autoincrement())` on non-IDENTITY columns)
5. History Card 360° journey, Requisition Pending ops UI, exports hub

---

## Commits (today)

| Time | SHA | Message |
|------|-----|---------|
| 18:31 | `5c60fae` | feat: PO finance, role permissions, pricing workflow, and GRN reliability |

Remote: `https://git.sukierp.com/RoshiniV/suki_tools.git` → `main`  
Stats: **60 files**, **+8547 / −692**

---

## Part A — Schema & migrations (additive, Tools-owned)

Three SQL Server migrations (safe / idempotent `IF` guards; no destructive ERP table rewrites):

| Migration | Purpose |
|-----------|---------|
| `20260808160000_create_tools_role_permission` | `TOOLS_ROLE_PERMISSION` — role × permission_key matrix |
| `20260808170000_tools_price_master_approval_workflow` | Pricing columns: `PROPOSED_RATE`, submit/approve audit, `REJECTED_REASON`; blank statuses → `APPROVED` |
| `20260808180000_tools_po_finance` | `TOOLS_PO_FINANCE` + `TOOLS_PO_FINANCE_LINE` (payment status, ledger/budget per line) |

**Prisma notes**
- `CommonPurchaseOrder` / `CommonPurchaseItem`: Tools may **create** (same ERP tables); `ROW_ID` / `GIR_NO` are **not** IDENTITY — allocate explicitly
- Seed script: `npm run db:seed:role-permissions` → `prisma/seed-role-permissions.ts`

---

## Part B — Role permissions & users (Settings)

### B1. Permission runtime

| Piece | Path |
|-------|------|
| Flag keys + labels | `src/lib/rolePermissions.ts` |
| DB cache (30s TTL) + Admin full-access | `src/lib/permissionsCache.ts` |
| `requirePermission` | `src/lib/auth.ts` |
| `/api/auth/me` returns `permissions` | `src/app/api/auth/me/route.ts` |
| Client `can(...)` | `src/lib/SessionContext.tsx` |

**New/extended flags (among others):** `canManageUsers`, `canApprovePricing`, `canCreatePO`, `canUpdateFinance`.

### B2. APIs & UI

| Piece | Path |
|-------|------|
| CRUD matrix API | `src/app/api/roles-permissions/route.ts` |
| App users API | `src/app/api/users/route.ts` |
| Roles / Permissions / Users pages | `src/app/dashboard/settings/{roles,permissions,users}/page.tsx` |
| Approval workflow settings | `src/app/dashboard/settings/approval-workflow/page.tsx` |

**Smoke:** Settings → Roles/Permissions edits persist; `/api/auth/me` reflects flags; Viewer cannot hit gated PO/pricing actions.

---

## Part C — Pricing Master approval workflow

**Rule:** Live `RATE` (what GRN / Issue / Tool Master read) changes **only on approve**. Edits write `PROPOSED_RATE` + `PENDING`.

| Piece | Path |
|-------|------|
| Propose / list | `src/app/api/pricing/route.ts` |
| Approve | `src/app/api/pricing/[id]/approve/route.ts` (`canApprovePricing`) |
| Reject | `src/app/api/pricing/[id]/reject/route.ts` |
| Masters UI | `src/app/dashboard/masters/pricing/page.tsx` |
| Validators | `PricingProposeSchema`, `PricingRejectSchema` in `src/lib/validators.ts` |

**Smoke:** Propose rate → live RATE unchanged → Approve copies proposed → RATE; Reject stores reason, stays off live rate.

---

## Part D — Purchase Order create + finance (Phase 2–3)

### D1. ERP-aligned PO numbering

| Piece | Path |
|-------|------|
| Prefix from `OTHER_TOOLS_TYPE.PO_PREFIX` | `src/lib/poNumbering.ts` |
| `allocateNextPoOrderNo` / item `ROW_ID` / GRN `GIR_NO` | same (UPDLOCK / MAX+1) |

Format example: `PO/GC-J0317` (GENERAL CONSUMABLES default).

### D2. Create PO into shared ERP tables

| Piece | Path |
|-------|------|
| POST create + enriched list | `src/app/api/po/route.ts` (`canCreatePO`) |
| Goods types | `src/app/api/po/goods-types/route.ts` |
| Approved rate lookup | `src/app/api/po/tool-rate/route.ts` |
| Spend helper | `src/app/api/po/spend/route.ts` |
| Create UI | `src/app/dashboard/po-linked/purchase-order/create/page.tsx` |
| List UI | `src/app/dashboard/po-linked/purchase-order/page.tsx` |

Lines support tool search, qty/rate (pricing or manual), optional ledger/budget.

### D3. Tools-owned finance (no FK into ERP PO tables)

| Piece | Path |
|-------|------|
| Payment / remarks update | `src/app/api/po/[id]/finance/route.ts` (`canUpdateFinance`) |
| GL codes from finance ledger | `src/app/api/gl-codes/route.ts` |
| PO reports / History Card PO views | `reports/purchase-orders`, `tools-history-card/purchase-orders` |

**Smoke:** Create PO → number follows prefix sequence; finance row tracks UNPAID/PAID without touching `COMMON_PURCHASE_*` schema.

---

## Part E — GRN reliability & receive UX

| Change | Detail |
|--------|--------|
| Schema | `ToolsPoReceive.girNo` / `ToolsPoReceiveTrans.rowId` — remove false `@default(autoincrement())` |
| Create | Explicit `allocateNextGirNo` + `allocateGrnTransRowIds` |
| List GET | Pagination (`page` / `pageSize`), filters (status, dates, supplier), search by GIR / PO / supplier / **tool no on lines** |
| UI | `src/app/dashboard/po-linked/receive/page.tsx` hardened |

**Smoke:** Create GRN without IDENTITY insert failures; search e.g. `OTH_J00289` finds receive rows.

---

## Part F — Requisition Pending + Issue write-back

| Piece | Path |
|-------|------|
| API (filters, counts, create) | `src/app/api/requisition-pending/route.ts` |
| Ops UI (aging, qty bars, drawer, deep-link to Issue) | `src/app/dashboard/transactions/requisition-pending/page.tsx` (~+1.1k) |
| Issue from pending + header status sync | `src/app/dashboard/transactions/issue/page.tsx`, `src/app/api/issue/route.ts` |

**Flow:** Pending line → Issue For Tools (`requisitionPending=Yes&reqNo=…`) → qty write-back → header OPEN/CLOSED when fulfilled.

---

## Part G — Tool Journey + Download Center + masters polish

### G1. 360° Tool Journey

| Piece | Path |
|-------|------|
| Aggregate events (purchase, GRN, issue, receive, calib, status) | `src/lib/toolJourney.ts` |
| API | `src/app/api/tools-history/[toolNo]/journey/route.ts` |
| Timeline UI | `src/components/ToolJourneyTimeline.tsx` |
| History Card tab | `src/app/dashboard/tools-history-card/page.tsx` |

### G2. Download Center

- `src/components/DownloadCenter.tsx` — category exports (tools, calibration, suppliers, subcontractors, tools-history) as xlsx/pdf
- Wired from reports / related surfaces

### G3. Other UI / API polish in same commit

- Calibration Frequency master: full CRUD UI (`masters/calib-frequency`)
- Purchase overview / KPI / ReportCharts tweaks
- Calibration results-update / tools `[id]` / approvals route updates
- `erpActor` / session / prisma small fixes

---

## Files touched (by area)

| Area | Examples |
|------|----------|
| Migrations + schema | `prisma/migrations/20260808*`, `prisma/schema.prisma`, `seed-role-permissions.ts` |
| Auth / perms | `permissionsCache.ts`, `rolePermissions.ts`, `auth.ts`, `SessionContext.tsx`, `api/auth/me`, `api/roles-permissions`, `api/users` |
| Pricing | `api/pricing/**`, `masters/pricing/page.tsx` |
| PO / finance / GRN | `poNumbering.ts`, `api/po/**`, `api/gl-codes`, create + list + receive pages |
| Requisition / issue | `api/requisition-pending`, `transactions/requisition-pending`, `transactions/issue`, `api/issue` |
| Journey / export | `toolJourney.ts`, `ToolJourneyTimeline.tsx`, `DownloadCenter.tsx` |
| Settings | `settings/{roles,permissions,users,approval-workflow}` |
| Validators | `src/lib/validators.ts` |

---

## Open / follow-ups

- [ ] Run migrations + `db:seed:role-permissions` on each environment if not already applied
- [ ] Confirm live ERP PO prefix letter for current FY still matches `OTHER_TOOLS_TYPE`
- [ ] Without-PO GRN / finance posting still out of scope (see `docs/erp-gap-analysis.md`)
- [ ] Optional: commit this worklog if dated logs should stay in `docs/`
- [ ] Figma login-page exploration (side thread today) — not in `5c60fae`

---

## End of day

| Item | Status |
|------|--------|
| Tools role permissions (DB + Settings) | ✅ Pushed (`5c60fae`) |
| Pricing propose / approve / reject | ✅ Pushed |
| PO create (ERP tables + numbering) | ✅ Pushed |
| PO finance (Tools-owned payment/ledger) | ✅ Pushed |
| GRN allocate + list search/pagination | ✅ Pushed |
| Requisition Pending + Issue write-back | ✅ Pushed |
| Tool Journey timeline | ✅ Pushed |
| Download Center | ✅ Pushed |
| Calib Frequency CRUD UI | ✅ Pushed |
| Worklog 08 Aug | ✅ This file (local until committed) |
